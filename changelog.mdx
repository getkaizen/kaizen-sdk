# Changelog

Short, chronological notes about SDK and doc updates. Newest entries appear first.

## v0.1.1
- Added full Mintlify doc structure: overview, installation, auth, quick start, core concepts, API methods, advanced usage, recipes, error reference, FAQ.
- Rewrote SDK reference pages with consistent templates and added a `health` endpoint doc.
- Clarified provider wrapper requirements and authentication guidance.

## v0.1.0
- Initial Python SDK release with support for:
  - `compress` / `decompress`
  - `optimize`
  - `prompts_encode` / `prompts_decode`
  - `optimize_request` / `optimize_response`
  - `health`
