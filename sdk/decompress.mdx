
title: Decompress
---

## `client.decompress(payload)`

Decompress a previously compressed payload back to human-readable format.

### Example
```python
decompressed = await client.decompress({
    "compressed": "..."
})
```
