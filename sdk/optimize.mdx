title: Optimize
---

## `client.optimize(payload)`

Optimizes the full payload (model + messages) by trimming tokens.

### Example
```python
optimized = await client.optimize({
    "model": "gpt-4",
    "messages": [...]
})
```
