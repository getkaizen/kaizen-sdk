
## `client.optimize_request(payload)`

Optimize **only the request** portion of a message interaction. Useful before sending to LLM.

### Example
```python
optimized = await client.optimize_request({
    "messages": [...]
})
```
